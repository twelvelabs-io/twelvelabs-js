/**
 * This file was auto-generated by Fern from our API Definition.
 */

import * as TwelvelabsApi from "../index";

/**
 * Metadata for document embeddings. Only Marengo 3.5 returns this object.
 */
export interface AsyncDocumentMetadata {
    /** The publicly accessible URL for the document file. */
    inputUrl?: string;
    /** The name of the document file. */
    inputFilename?: string;
    /** The `embedding_option` values used to generate the embedding. */
    embeddingOptions?: string[];
    /** The `embedding_scope` values used to generate the embedding. */
    embeddingScopes?: TwelvelabsApi.AsyncDocumentMetadataEmbeddingScopesItem[];
}
